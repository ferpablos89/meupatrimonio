services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/var/www
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: finance
      POSTGRES_USER: finance
      POSTGRES_PASSWORD: finance
